Trabalhar a issue #288 do palpiteiro-claude: Seam OddsProvider + DTO NormalizedOdds (ADR 0025). 1ª da fila pós-#245 (desbloqueada; destrava #289→#290). É refactor PURO, byte-idêntico — não é feature.

Ler primeiro: docs/handoffs/HANDOFF-288.md (auto-suficiente, line refs verificados 2026-06-16, seção "Ancoragem no código"); depois gh issue view 288 -R fernandolisboa/palpiteiro-claude (6 ACs) + docs/decisions/0025-api-football-provider-de-odds-duplo-provider.md.

Núcleo: extrair OddsProvider (interface) + NormalizedOdds (DTO provider-neutro) ESPELHANDO SportsDataProvider/NormalizedFixture (lib/providers/sports-data/types.ts:166-212 / :22-40). The Odds API vira o 1º adapter. Os 3 consumidores (lib/odds/fetch-and-snapshot.ts, lib/ai/predict.ts, lib/odds/select-bookmaker.ts) + lib/odds/market-descriptor.ts passam a depender do DTO; tirar providerMarketKey e resolveSelectionKey(OddsApiOutcome) do descriptor PRESERVANDO dbMarketKey/dbSelectionKey/coveredLeagues/oddsSource/impliedSumTarget. Errata: .env.example:42 + ADR 0005:15 (Pro 7500/dia, não free 100); revisar (não corrigir cego) o comentário region=eu de odds-api-constants.ts:52-53 vs #158.

LANDMINE: byte-idêntico é INEGOCIÁVEL — a rede são os testes de paridade/golden (persisted-parity, scenario, select-bookmaker, market-descriptor, implied-probability, fetch-and-snapshot.pglite, odds-api*.test, odds-card-parity.golden), devem ficar verdes SEM editar asserções; se uma asserção mudar, o comportamento mudou → pare. SEM migration (não rode db:generate). Wire types do The Odds API viram PRIVADOS do adapter (zero re-export). Manter o wrapper binário pickBestTotalsBookmaker (callers legados) e a ref direta do OVER_UNDER_ALT (cartucho v3). O acoplamento mais duro é select-bookmaker.ts:77-85 (mercado completo de um único book) — exatamente o que #289 vai estressar. Label só pos-pivot. CI verde (pglite flaka local multi-core → --no-file-parallelism).

Flow: explorar (fresco) → plano → plan-review → implementação → code review → merge verde. Critério: os 6 ACs do #288. Cluster: #288 → #289 → #290 → resto pós-pivot → #246 (polish visual, por último, com impeccable).