# PLAN — #176 Dupla chance (double_chance) — REWORKED pós plan-gate (2 rounds)

> #176 SOZINHA. Worktree `.claude/worktrees/176` off `origin/main @ f94795c4`. Branch `feat/176-double-chance`.
> Fonte de verdade: `docs/specs/double-chance-prompt-design.md` (keys/settlement/output) + ADR 0018 (edge).
> N=3, espelha `match_result`/1X2 + odds *additional* por evento (caminho btts/#174) + gate `coveredLeagues:['world_cup']`.

## Decisões FINAIS (spec + usuário)
- **selectionKeys (spec, ordem-contrato):** `["home_or_draw","away_or_draw","home_or_away"]` = 1X / X2 / 12. sort 0/1/2.
- **Labels PT (por extenso, seed-pinned, byte = presentation):** market "Dupla chance"; `home_or_draw`="Casa ou empate", `away_or_draw`="Empate ou fora", `home_or_away`="Casa ou fora".
- **Provider (VALIDADO):** providerMarketKey `'double_chance'`; outcomes sem `point`; nomes compostos ordem-livre: `"{home} or Draw"`→home_or_draw, `"{away} or Draw"`→away_or_draw, `"{teamA} or {teamB}"`→home_or_away.
- **Math (usuário = de-vig honesto Σ=2):** as 3 duplas se sobrepõem (Σ prob real ≈ 200%). `MarketDescriptor.impliedSumTarget?: number` (default 1; double_chance=2). Implícita por seleção (%) escalada por `impliedSumTarget` em DOIS sites de consumo: predict (impliedByKey) E `computeMarketScenarios` (novo param, default 1, vindo do descriptor via `toAnalysisView`). **Core `computeMarketImpliedProbabilities` INTOCADO** (Σ=1; select-bookmaker usa default → paridade). LLM dá probs de par HONESTAS (somam ~200); `selectionProbs` retorna identidade (sem normalizar). edge = model − implied (ambos Σ~200). confidence_pct = prob honesta do par recomendado (~92% p/ favorito). View: modelProb persistido (Σ200) + implied re-derivado×2 (Σ200) → edge da grade == edge persistido. **SEM coherence guard** (espelha match_result; edge é per-seleção, robusto). Emenda ADR 0018 + reconciliar o spec.
- **Output:** recommendation ∈ {home_or_draw, away_or_draw, home_or_away, pass} + 3 `prob_*` (0-100) + confidence_pct + rationale + key_factors + minimum_odd (omit em pass). pass → confidence_pct reporta prob_home_or_draw (espelha match_result).
- **Settlement (rule_key `double_chance`):** resultado=h>a?home:h<a?away:draw; allowed: home_or_draw→{home,draw}, away_or_draw→{draw,away}, home_or_away→{home,away}; won=set.has(resultado). Sem push. null-split→SettlementError (defensivo/parity). **key desconhecida→SettlementError** (precedente "prefer skip"; inalcançável em prod pois vem do join de market_selections — documentar).
- **Flags:** is_active=true, is_graduated=false (admin-only). coveredLeagues `['world_cup']` (Brasileirão re-check #158 fora desta fatia).

## Blocker pré-requisito (validação live)
`BookmakerSchema.last_update` é `z.string()` obrigatório; payload real dc (1xBet) OMITE no nível bookmaker → `getOddsForEvent` lança → dc nunca tem odds (ameaça btts também). **Fix:** `odds-api-schemas.ts` → bookmaker `last_update: z.string().optional()` (market-level FICA obrigatório; é o consumido em select-bookmaker.ts:88). Único consumidor prod = market-level → cirúrgico.

## Commits (cada um VERDE: typecheck+lint+vitest dos paths). Ordem ajustável se um teste forçar bundling.
1. **`fix(odds): bookmaker.last_update opcional`** — schema + teste (aceita bookmaker sem last_update).
2. **`feat(odds): de-vig não-partição via impliedSumTarget`** — campo opcional no `MarketDescriptor`; escala impliedByKey no predict; param `impliedSumTarget?` em `computeMarketScenarios` (default 1) + `toAnalysisView` resolve do descriptor por marketKey (helper client-safe; market-descriptor não importa AI/prompt). Core intocado. Testes: 1X2/over_under golden inalterados + N=3 Σ=2.
3. **`feat(db): enum recommendation += dupla chance + widening`** — `recommendationEnum` += 3 keys em schema.ts; `db:generate` (DATABASE_URL dummy) → `0017_*.sql` (ALTER TYPE ADD VALUE, isolado, sem seed). **Mesmo commit:** widen as 3 closed unions (analysis.ts `PredictionInput.recommendation`, kpis.ts `DashboardRow.recommendation`, recent-prediction.ts `RecentInput.recommendation`) — senão typecheck quebra (precedente btts). **SEM stash-dance** (cast em predict é string→union; só pglite lê migration real, mesmo PR).
4. **`feat(odds): descriptor DOUBLE_CHANCE`** — selectionKeys, dbMarketKey/providerMarketKey 'double_chance', oddsSource:'additional', coveredLeagues:['world_cup'], `impliedSumTarget:2`, params:null, `resolveSelectionKey` defensivo, +ALL_DESCRIPTORS. **NÃO** add settlementRuleKey (não é campo do descriptor). Testes: resolveSelectionKey (3 nomes reais + ordem invertida + acento + NÃO-casa→null + >2 partes→null) + market-catalog (world_cup inclui dc / não-coberta exclui). [Se houver teste que exige cartucho p/ todo descriptor → bundle com commit 5.]
5. **`feat(ai): cartucho double_chance_v1 (N=3)`** — `lib/ai/markets/double_chance/{schemas,prompt,user-message,build-input,index}.ts` espelhando match_result; `DOUBLE_CHANCE_VERSION='double_chance_v1'`; re-exports module-binding; registra em registry.ts. Prompt: 1X/X2/12, probs honestas (somam ~200, NÃO 100), `${MIN_EDGE_PP} pontos percentuais`, "odds baixas → pass é default forte; não relaxar floor". Testes: schemas + user-message(+MIN_EDGE_PP sync) + marketkey-parity loop (getCartridge('double_chance')).
6. **`feat(settlement): regra double_chance (allowed-set)`** — `rules/double_chance.ts` + registry. Testes (parity btts/match_result): 3 cenários (home-win/away-win/draw) × allowed-set, never-push, null-split→SettlementError, unknown-key→SettlementError, getSettlementRule('double_chance')===rule, computeSettlement e2e (pass→void, won profit hand-calc, lost).
7. **`feat(db): seed double_chance + presentation`** — `0018` via `drizzle-kit generate --custom --name seed_double_chance`: INSERT markets (key/settlement_rule_key/label/is_active=true/is_graduated=false) + 3 market_selections (key/label/sort_order), `ON CONFLICT DO NOTHING`. presentation.ts entry (labels byte=seed; betSummary plain '' como match_result) + settlementMetricValue (placar, como 1X2). Testes: presentation.test.ts dc case + seed-parity pglite dc block (labels + sort_order 0/1/2).
8. **`test(ai): predict + action gate double_chance`** — predict e2e (espelha predict.match-result.test.ts; freshSnapshot3Way keyed pelas dc keys; edge Σ=2; uma rec/pass; ai_calls promptVersion double_chance_v1) + predictions.test.ts gate (admin+dc world_cup→threaded+pre-warm order; liga não-coberta→coage over_under antes do spend; finished→erro sem spend).
9. **`docs: ADR 0018 emenda (não-partição) + ADR 0015 ativação dc + reconciliar spec`** + MARKETS.DOUBLE_CHANCE (decorativo, parity).

## Invariantes (não regridem) — prova final
- `grep -rEn "=== ['\"]double" lib app` (sem testes/seed) → nada. Dispatch só por descriptor/identidade existente.
- Paridade over/under E 1X2 byte-idêntica (impliedSumTarget default 1; view binária congelada intacta; nada persistido muda).
- Edge N-vias (ADR 0018 + emenda); Zod em todo output; predict.ts única porta LLM; expand-migrate-contract; quota additional só por evento (NUNCA batch); pre-warm só no analyze, após short-circuit de analisabilidade.

## Saída
dc ATIVO em prod atrás de flag (admin, world_cup) pelo caminho genérico; análise ponta-a-ponta; settlement testado; paridade intacta; Vercel + prod deploy verdes (migration=ativação). Code-review find→verify como PR comment; ressalvas value-adding fixadas. Atualizar HANDOFF + kickoff do #175. Backtest≥20 NÃO roda (PAGO/acumula). Quota validação: 4 créditos (remaining 439).
